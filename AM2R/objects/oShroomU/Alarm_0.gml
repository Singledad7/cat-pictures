/*switch (floor(image_angle)) {
    case 180: { // out of right wall
        image_yscale = -1;
        break;
    }
    case 90: {
        image_yscale = choose(-1, 1);
        // add sprite
    }
    case 270: {
        image_yscale = choose(-1, 1);
        // add sprite
    }
    default: {
        // do things
        break;
    }
}

/* */
/*  */
